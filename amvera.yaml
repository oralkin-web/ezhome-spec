meta:
  environment: node
  toolchain:
    name: npm
    version: 20
build:
  buildCommands:
    - cd client && npm ci && npm run build
run:
  containerPort: 3000
  command: npx serve -s client/dist -l 3000
